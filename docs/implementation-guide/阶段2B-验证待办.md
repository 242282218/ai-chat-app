# Phase 2B 验证待办清单

**状态**: ⏳ 等待测试环境
**创建日期**: 2026-06-15
**优先级**: 🔴 高 - 这是关键决策点

---

## ⚠️ 重要提示

Phase 2B 是整个迁移的**关键决策点**。在继续 Phase 3（组件迁移）之前，必须完成这些验证。

**不要跳过 Phase 2B！** 否则可能在 Phase 3 迁移一半时发现问题，导致大量返工。

---

## 前置条件

- [ ] 有可用的 Android 设备或模拟器（API 26+）
- [ ] 可以编译和安装 APK
- [ ] 可以查看 Logcat 日志
- [ ] （可选）性能分析工具：Android Profiler / adb shell

---

## 1. 功能验证（60 分）

### 1.1 基本渲染测试（10 分）

**目标**: 验证 StreamChatScreen 可以正常显示

- [ ] **步骤**: 临时修改导航，进入 StreamChatScreen
  ```kotlin
  // 在某个测试按钮中添加
  navController.navigate("stream_chat/test_conversation_id")
  ```
- [ ] **预期**: 界面正常渲染，不崩溃
- [ ] **检查**: 顶部栏、消息列表、输入框都显示正常
- [ ] **评分**: 全部正常 = 10 分，部分问题 = 5 分，崩溃 = 0 分

### 1.2 数据转换测试（10 分）

**目标**: 验证 MessageConverter 能正确转换 Domain Message

- [ ] **步骤**: 在现有对话中查看消息
- [ ] **预期**: Room 中的消息正确转换为 Stream Message 显示
- [ ] **检查**:
  - 消息内容正确
  - 发送者（User/Assistant）正确
  - 时间戳正确
- [ ] **评分**: 全部正确 = 10 分，有错误 = 0 分

### 1.3 消息发送测试（10 分）

**目标**: 验证可以发送消息到 Room

- [ ] **步骤**: 在输入框输入文本并发送
- [ ] **预期**:
  - 消息立即显示在列表中
  - 消息保存到 Room 数据库
  - 可以在原有 ChatScreen 中看到同样的消息
- [ ] **检查**: 数据库一致性
- [ ] **评分**: 全部正常 = 10 分，部分失败 = 5 分，完全失败 = 0 分

### 1.4 流式响应测试（10 分）

**目标**: 验证 AI 响应可以流式显示（即使是模拟的）

- [ ] **步骤**: 发送消息，观察 AI 响应
- [ ] **预期**:
  - AI 响应逐字显示（打字机效果）
  - 最终完整消息保存到 Room
- [ ] **检查**: 流畅度、完整性
- [ ] **评分**: 流畅 = 10 分，卡顿 = 5 分，无效果 = 0 分

### 1.5 Markdown 渲染测试（10 分）

**目标**: 验证 Markdown 内容正确渲染

- [ ] **步骤**: 发送包含 Markdown 的消息（代码块、粗体、列表等）
- [ ] **预期**: Stream SDK 的 Markdown 渲染器正常工作
- [ ] **检查**:
  ```markdown
  **粗体** *斜体*
  - 列表项 1
  - 列表项 2

  ```kotlin
  fun test() {
      println("Hello")
  }
  ```
  ```
- [ ] **评分**: 完美渲染 = 10 分，部分支持 = 5 分，不支持 = 0 分

### 1.6 主题一致性测试（10 分）

**目标**: 验证 AiChatStreamTheme 颜色正确

- [ ] **步骤**: 对比 StreamChatScreen 和原有 ChatScreen
- [ ] **预期**:
  - 主色调（emerald）一致
  - 背景色一致
  - 消息气泡颜色一致
  - 文字颜色一致
- [ ] **评分**: 完全一致 = 10 分，略有差异 = 5 分，差异大 = 0 分

---

## 2. 性能验证（25 分）

### 2.1 APK 体积（5 分）

- [ ] **命令**:
  ```bash
  ls -lh app/build/outputs/apk/debug/app-debug.apk
  ```
- [ ] **目标**: ≤ +2MB 相比 Phase 1
- [ ] **当前**: Phase 1 APK = 30.51MB
- [ ] **实际**: _________ MB
- [ ] **评分**:
  - ≤ 32.5MB = 5 分
  - 32.5-35MB = 3 分
  - > 35MB = 0 分

### 2.2 启动时间（5 分）

- [ ] **命令**:
  ```bash
  adb shell am start -W com.aichat.workbench/.MainActivity
  ```
- [ ] **目标**: ≤ +200ms
- [ ] **基准**: 需要先测试原版本
- [ ] **实际**: _________ ms
- [ ] **评分**:
  - ≤ +200ms = 5 分
  - +200-400ms = 3 分
  - > +400ms = 0 分

### 2.3 内存占用（10 分）

- [ ] **命令**:
  ```bash
  adb shell dumpsys meminfo com.aichat.workbench
  ```
- [ ] **目标**: ≤ +50MB
- [ ] **检查**:
  - 空闲状态内存
  - 加载 100 条消息后内存
  - 快速滚动时内存
- [ ] **评分**:
  - ≤ +50MB = 10 分
  - +50-100MB = 5 分
  - > +100MB = 0 分

### 2.4 滚动性能（5 分）

- [ ] **命令**:
  ```bash
  adb shell dumpsys gfxinfo com.aichat.workbench
  ```
- [ ] **目标**: ≥ 55 FPS
- [ ] **场景**: 快速滚动 100+ 条消息
- [ ] **评分**:
  - ≥ 55 FPS = 5 分
  - 45-55 FPS = 3 分
  - < 45 FPS = 0 分

---

## 3. 用户体验对比（15 分）

### 3.1 视觉对比（10 分）

- [ ] **方法**: 截图对比 StreamChatScreen 和 ChatScreen
- [ ] **检查项**:
  - [ ] 整体布局相似度
  - [ ] 消息气泡样式
  - [ ] 字体大小和间距
  - [ ] 颜色一致性
  - [ ] 圆角和阴影
- [ ] **评分**:
  - 几乎一致 = 10 分
  - 明显差异但可接受 = 5 分
  - 差异大 = 0 分

### 3.2 交互流畅度（5 分）

- [ ] **测试**:
  - 输入响应速度
  - 消息发送反馈
  - 滚动流畅度
  - 无明显卡顿或延迟
- [ ] **评分**:
  - 非常流畅 = 5 分
  - 偶尔卡顿 = 3 分
  - 经常卡顿 = 0 分

---

## 4. 决策评分

### 总分计算

- 功能验证: _____ / 60
- 性能验证: _____ / 25
- 用户体验: _____ / 15
- **总分**: _____ / 100

### 决策标准

- ✅ **总分 ≥ 85**: 继续 Phase 3（组件迁移）
- ⚠️ **总分 70-84**: 调整方案，优化问题后再评估
- ❌ **总分 < 70**: 考虑回退，Stream SDK 可能不适合

### 主要问题记录

如果总分 < 85，记录主要问题：

1. _________________________________
2. _________________________________
3. _________________________________

---

## 5. 下一步行动

### 如果继续（总分 ≥ 85）

1. 阅读 `03-阶段2B-验证测试.md` 完整文档
2. 执行 `04-阶段3-组件迁移.md`
3. 逐步替换现有组件

### 如果需要调整（总分 70-84）

1. 针对问题进行优化
2. 重新测试
3. 达到 85 分后继续

### 如果回退（总分 < 70）

1. 阅读 `07-回滚方案.md`
2. 执行完全回滚
3. 考虑其他方案

---

## 附：测试环境准备

### 最小测试环境

如果没有物理设备，可以使用：

1. **Android Studio 模拟器**
   ```bash
   # 创建 AVD
   # API 26+, 推荐 API 34
   # x86_64 架构
   # 至少 2GB RAM
   ```

2. **GitHub Actions + Firebase Test Lab**（如果项目支持）
   ```yaml
   # .github/workflows/test-stream-ui.yml
   # 自动化测试流程
   ```

3. **测试机远程访问**
   - 文档中提到了测试机: 45.207.195.235
   - 如果可以访问，可以在测试机上运行

### 快速验证脚本

创建一个快速验证脚本：

```bash
#!/bin/bash
# scripts/test/phase2b-quick-check.sh

echo "=== Phase 2B 快速验证 ==="

echo "1. 构建 APK..."
./gradlew assembleDebug

echo "2. 安装到设备..."
adb install -r app/build/outputs/apk/debug/app-debug.apk

echo "3. 启动应用..."
adb shell am start -W com.aichat.workbench/.MainActivity

echo "4. 查看日志（Ctrl+C 退出）..."
adb logcat -s StreamChatInitializer:* StreamChatViewModel:*

echo "完成！现在手动测试功能。"
```

---

**状态跟踪**: 记得在完成后更新本文档顶部的状态！
