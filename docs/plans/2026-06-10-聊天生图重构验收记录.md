# 聊天生图重构验收记录

日期：2026-06-10  
分支：`main`  
基准提交：`35e30ed`  
状态：本地阶段验收通过；远端 GitHub Actions 待提交后触发。

## 阶段结果

| 阶段 | 结果 | 证据 |
| --- | --- | --- |
| Phase 0 范围冻结 | 已完成 | 已读取执行文档、README、优化路线图、同类项目调研和代码索引；旧边界按“不做/兼容迁移/历史记录”分类。 |
| Phase 1 仓库和文档收敛 | 已完成 | `gateway/`、`contracts/` 不存在；根目录旧崩溃/CI 日志无残留；英文旧文档改为中文文件名。 |
| Phase 2 大文件拆分 | 已完成 | Provider 设置、生图页、Workbench 通用组件已按职责拆分；baseline profile 已同步新组件类名。 |
| Phase 3 模型连接体验 | 已完成 | 底部入口改为“模型”；Provider 摘要展示默认聊天模型、默认图片模型和图片能力连接数。 |
| Phase 4 聊天主路径 | 已完成 | 会话列表和聊天页均能在无可用聊天模型时进入模型配置；流式、停止、失败重试由现有测试覆盖。 |
| Phase 5 生图主路径 | 已完成 | 提示词优先、参数折叠、作品库动作、失败复用/重试/转聊天路径由实现和测试覆盖。 |
| Phase 6 视觉和交互一致性 | 已完成 | 状态色集中到 `StatusTone`；错误通知具备语义提示；图标按钮保持 48dp、tooltip 和 content description。 |
| Phase 7 回归和门禁 | 本地通过，远端待跑 | 本地执行 CI 等价命令通过；未自动 commit/push，因此不能证明当前未提交 diff 的远端 CI 绿色。 |

## 验证命令

```powershell
.\gradlew.bat testDebugUnitTest lint assembleDebug assembleRelease --no-daemon --stacktrace
.\gradlew.bat testDebugUnitTest --tests "*AiChatMigrationTest*" --no-daemon --stacktrace
```

结果均为 `BUILD SUCCESSFUL`。

安装包签名验证：

```powershell
& "E:\Android\Sdk\build-tools\35.0.0\apksigner.bat" verify --verbose --print-certs app\build\outputs\apk\debug\app-debug.apk
```

结果：`Verifies`，APK Signature Scheme v2 为 `true`，签名证书为 `Android Debug`。

补充已单独跑过的窄范围验证：

```powershell
.\gradlew.bat testDebugUnitTest --tests "*ProviderDraftsTest" --no-daemon --stacktrace
.\gradlew.bat testDebugUnitTest --tests "*ConversationsViewModelTest" --no-daemon --stacktrace
.\gradlew.bat testDebugUnitTest --tests "*ChatViewModelTest" --tests "*ChatInputStatusTest" --no-daemon --stacktrace
.\gradlew.bat testDebugUnitTest --tests "*ImageDraftsTest" --tests "*ImageGenerationViewModelTest" --tests "*GenerateImageUseCaseTest" --no-daemon --stacktrace
```

## 静态检查

- `rg` 旧入口结果只剩执行文档、调研/路线图中明确“不做”的说明，以及 Room 迁移兼容测试中的旧数据。
- `docs/` 下当前文件名均带中文描述。
- 当前 diff 未修改 `app/schemas/**`、`AiChatDatabase`、entity、DAO 或迁移文件。
- 生成产物包括 `app/build/outputs/apk/debug/app-debug.apk`。
- `app/build/outputs/apk/release/app-release-unsigned.apk` 只是 release 构建检查产物；没有正式签名配置时不作为 GitHub Release 安装包发布。

## 待外部动作

- 需要用户明确允许 commit/push 后，才能触发 GitHub Actions 对当前变更跑远端 CI。
- 本轮不创建 tag、不创建 GitHub Release、不上传 APK。
