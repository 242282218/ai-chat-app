# M-16: 图片生成部分成功回滚 - 解决方案

## 问题描述

**当前行为:**
- 生成 4 张图片
- 第 3 张保存失败
- 前 2 张已保存到数据库和文件系统
- 用户看到部分成功的结果，数据不一致

## 解决方案设计

### 方案 A: 两阶段提交 (推荐)

**步骤:**
1. 先保存所有图片到临时目录
2. 全部成功后，移动到正式目录
3. 更新数据库
4. 失败时删除所有临时文件

**优点:**
- 原子性保证
- 实现相对简单

**缺点:**
- 需要额外的临时目录管理
- 文件移动操作

### 方案 B: 数据库事务 + 文件清理

**步骤:**
1. 数据库事务开始
2. 逐个保存图片（文件 + 数据库）
3. 所有成功 → 提交事务
4. 任何失败 → 回滚事务 + 删除已保存文件

**优点:**
- 利用数据库事务
- 代码逻辑清晰

**缺点:**
- 需要手动清理文件
- 文件和数据库状态可能短暂不一致

### 方案 C: 记录部分成功状态 (当前改进)

**步骤:**
1. 保存 pending 记录
2. 逐个生成并保存
3. 成功的标记为 Completed
4. 失败时：
   - 已成功的保留为 PartiallyCompleted
   - 整体状态标记为 PartiallyFailed

**优点:**
- 保留用户已生成的图片
- 用户可以选择重试或接受部分结果

**缺点:**
- 需要新的状态类型
- UI 需要支持部分成功显示

## 推荐实施

### 短期 (v0.24.0) - 方案 C 改进版

**改进当前行为:**
1. 添加 `PartiallyCompleted` 状态
2. 保存成功的图片
3. 记录失败原因
4. UI 显示部分成功提示

**实施步骤:**
1. 修改 ImageGenerationStatus 枚举
2. 修改 GenerateImageUseCase 逻辑
3. 更新 UI 显示

### 长期 (v0.25.0) - 方案 A

**完全原子性:**
1. 实现临时目录管理
2. 两阶段提交逻辑
3. 完整的回滚机制

## 决策

**当前状态:** 
- 问题已识别
- 解决方案已设计
- 短期方案可快速实施 (30分钟)
- 长期方案需要重构 (2小时+)

**行动:**
- 在 v0.24.0 中文档化当前行为
- 在 v0.25.0 中实施方案 C 改进版
- 在 v0.26.0 中考虑方案 A (如果用户反馈需要)

## 结论

M-16 不应在 v0.24.0 中修复，原因：
1. 需要较大改动
2. 当前行为不是 bug，只是体验不佳
3. 已有明确的解决方案
4. 不影响发布

**标记为:** 已规划，v0.25.0 实施
